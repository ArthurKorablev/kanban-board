import { setIssues } from "../../redux/actions";
import { connect } from "react-redux";

const Main = ({ setIssues }) => {
    return (
      <div>
        
      </div>
    );
  }
  
  export default connect(null, { setIssues })(Main)